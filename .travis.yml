language: node_js
node_js:
- "7"

cache: yarn

deploy:
  skip_cleanup: true
  provider: script
  script: yarn run gh-pages -d build -r https://$GH_TOKEN@github.com/QualitySoftwareDevelopers/qualitysoftwaredevelopers.github.io.git -b master
  on:
    branch: develop
